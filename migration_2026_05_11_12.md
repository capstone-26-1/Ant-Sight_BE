# AntSight 인프라 마이그레이션 작업 기록

> 작업 기간: 2026-05-11 ~ 2026-05-12
> 작업자: 백엔드 담당
> 목적: 단일 EC2 통합 구조 → API/크롤러/DB 분리 구조로 마이그레이션

---

## 1. 작업 요약

### Before
```
[EC2 1대 (기존)]
├── Spring Boot API (7689 포트)
├── MySQL (로컬)
└── Python 크롤러 (5분 cron)
```

### After
```
[크롤러 EC2 (기존, IP: 172.31.37.170)]
└── Python 크롤러 (5분 cron, --api-url로 새 EC2 호출)
                       ↓
[API EC2 (신규, IP: 54.180.134.253 / 사설 172.31.6.5)]
└── Spring Boot API (7689 포트)
                       ↓
[RDS (신규, antsight-db)]
└── MySQL 8.0 (stockboard DB)
```

---

## 2. 주요 작업 단계

### Phase 1: 데이터 마이그레이션

기존 EC2 MySQL → RDS로 이전.

```bash
# 기존 EC2에서 dump
mysqldump -u stockuser -p \
  --single-transaction \
  --no-tablespaces \
  --default-character-set=utf8mb4 \
  stockboard > stockboard_dump.sql

# RDS에 DB 생성
mysql -h <RDS_엔드포인트> -u admin -p -e \
  "CREATE DATABASE stockboard CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"

# 앱 전용 사용자 생성 (원격 접속용이라 '@%')
mysql -h <RDS_엔드포인트> -u admin -p << 'EOF'
CREATE USER 'stockuser'@'%' IDENTIFIED BY '비밀번호';
GRANT ALL PRIVILEGES ON stockboard.* TO 'stockuser'@'%';
FLUSH PRIVILEGES;
EOF

# 데이터 import
mysql -h <RDS_엔드포인트> -u admin -p stockboard < stockboard_dump.sql
```

검증: 기존 EC2의 `posts` 카운트와 RDS의 카운트가 일치 (cron이 계속 도는 동안 작업해서 약간 차이는 정상).

### Phase 2: 새 EC2에 Spring 배포

기본 환경:
```bash
# Swap 2GB (1GB RAM 보완)
sudo fallocate -l 2G /swapfile && sudo chmod 600 /swapfile
sudo mkswap /swapfile && sudo swapon /swapfile
echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab

# Java 21 + MySQL 클라이언트
sudo apt update && sudo apt install -y openjdk-21-jdk mysql-client
```

`application-prod.yml` 변경 (DB URL → RDS 엔드포인트):
```yaml
spring:
  datasource:
    url: jdbc:mysql://antsight-db.cb8ugsm46gwk.ap-northeast-2.rds.amazonaws.com:3306/stockboard?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: stockuser
    password: ${DB_PASSWORD}
```

systemd 등록 (`/etc/systemd/system/stockapi.service`):
```ini
[Unit]
Description=Stock Board Spring API
After=network.target

[Service]
Type=simple
User=ubuntu
WorkingDirectory=/home/ubuntu
Environment="DB_PASSWORD=<비밀번호>"
Environment="SPRING_PROFILES_ACTIVE=prod"
ExecStart=/usr/bin/java -Xms256m -Xmx512m -jar /home/ubuntu/app.jar
SuccessExitStatus=143
Restart=on-failure
RestartSec=10

[Install]
WantedBy=multi-user.target
```

### Phase 3: 보안그룹 설정

**RDS 보안그룹 인바운드**:
- MySQL/Aurora(3306) ← 본인 IP (개발용, 운영 시 제거)
- MySQL/Aurora(3306) ← API EC2 SG
- MySQL/Aurora(3306) ← 크롤러 EC2 SG

**새 API EC2 보안그룹 인바운드**:
- SSH(22) ← 내 IP
- 사용자 지정 TCP(7689) ← 크롤러 EC2 SG (사설 통신용)

⚠️ 한 보안그룹 규칙에 IP와 SG를 섞을 수 없음. 둘 다 허용하려면 줄을 두 개로 분리.

### Phase 4: 크롤러 cron 변경

기존 EC2의 crontab을 새 API EC2의 사설 IP를 가리키게 수정:

```cron
*/5 * * * * cd /home/ubuntu/crawler && /home/ubuntu/crawler/venv/bin/python pipeline.py --mode incremental --window-minutes 6 --api-url http://172.31.6.5:7689 >> /home/ubuntu/crawler/cron.log 2>&1
```

`localhost:7689` → `172.31.6.5:7689` (사설 IP 사용으로 데이터 전송 비용 0).

---

## 3. 마이그레이션 중 만난 문제와 해결

### 문제 1: saveBulk의 동시성 폭주
**증상**:
```
Duplicate entry '005930-코스피 급락 원인-2026.05.12 18:28' for key 'posts.uq_post'
```
가 같은 글에 대해 5초 사이 7번 반복.

**원인**:
1. 시장 급락 이벤트로 005930에 5분당 95건의 글 폭주 (평소 0~10건)
2. `saveBulk`는 한 트랜잭션으로 `saveAll` → 1건이 unique 위반이면 **전체 롤백**
3. 500 응답을 받은 크롤러가 `try_post_with_retry`로 재시도
4. `isolate_and_save`가 batch를 분할 재귀 호출
5. 같은 글이 분할 트리의 여러 노드에서 INSERT 시도됨

**해결**: `PostService.saveBulk`를 **건별 독립 트랜잭션**으로 변경.

```java
public PostDto.BulkResponse saveBulk(List<PostDto.PostRequest> requests) {
    int inserted = 0, skipped = 0;
    for (PostDto.PostRequest req : requests) {
        if (trySaveOne(req)) inserted++;
        else skipped++;
    }
    return new PostDto.BulkResponse(requests.size(), inserted, skipped);
}

@Transactional(propagation = Propagation.REQUIRES_NEW)
public boolean trySaveOne(PostDto.PostRequest req) {
    try {
        postRepository.saveAndFlush(req.toEntity());
        return true;
    } catch (DataIntegrityViolationException e) {
        return false;
    }
}
```

핵심:
- `existsBy...` 사전 체크 제거 → DB의 unique 제약에 위임
- `REQUIRES_NEW`로 각 글이 독립 트랜잭션 → 한 건 실패가 다른 건에 영향 없음
- `DataIntegrityViolationException` 잡아 skip 처리

이후 동일 batch에 중복이 섞여도 정상 처리됨.

```
배포 후 로그:
[PostService] bulk 저장 완료 - 총 13건 / 삽입 13건 / 중복 스킵 0건
[PostService] bulk 저장 완료 - 총 2건 / 삽입 1건 / 중복 스킵 1건  ← 중복 1건 skip, 나머지 정상
```

### 문제 2: 헬스체크 URL 혼선
**증상**: 팀원이 `/api/health` 호출 → Whitelabel Error 404 → "API가 안 된다"고 보고

**진단**: 404 응답이 왔다는 건 **서버 정상 동작 + URL만 없음**.
- 진짜 서버 죽음: Connection refused
- 네트워크 차단: Connection timeout
- 404 Whitelabel: 서버 살아있음 ✅

**현재 상태**: 헬스체크는 `/posts/health` (PostController 내부). `/api/health` 엔드포인트는 미존재.

**할 일**: 팀 합의 후 API prefix 규칙 정리 필요. `/api/...` 통일 vs 자원명 직접 사용.

---

## 4. 현재 인프라 상태

| 컴포넌트 | 위치 | 상태 |
|---|---|---|
| 크롤러 | 기존 EC2 (172.31.37.170) | ✅ 5분 cron 정상 |
| API | 새 EC2 (54.180.134.253 / 172.31.6.5) | ✅ Spring 정상 |
| DB | RDS (antsight-db) | ✅ MySQL 8.0 정상 |
| ant_index 컬럼 | `posts` 테이블 | ✅ 추가됨 (값은 NULL, AI 연동 대기) |

### 현재 운영 흐름

```
5분마다:
1. 크롤러 EC2의 cron 실행
2. 100종목 incremental 크롤링 (6분 윈도우)
3. 새 글들을 새 API EC2로 POST /posts/bulk
4. Spring이 RDS에 저장 (중복은 자동 skip)
```

### 헬스체크
```bash
curl http://localhost:7689/posts/health   # EC2 내부
curl http://54.180.134.253:7689/posts/health   # 외부 (보안그룹 허용 시)
```

## 8. 참고

### 변경된 파일
- `PostService.java` — saveBulk를 idempotent하게 수정
- `application-prod.yml` — DB URL을 RDS 엔드포인트로 변경
- `/etc/systemd/system/stockapi.service` (새 EC2) — 환경변수 설정
- 기존 EC2 crontab — `--api-url`을 새 EC2 사설 IP로

### 주요 IP/엔드포인트
| 자원 | 값 |
|---|---|
| 기존 EC2 (크롤러) | 172.31.37.170 (사설) |
| 새 EC2 (API) | 54.180.134.253 (퍼블릭), 172.31.6.5 (사설) |
| RDS | antsight-db.cb8ugsm46gwk.ap-northeast-2.rds.amazonaws.com:3306 |
| API 헬스체크 | http://localhost:7689/posts/health |
| API bulk 저장 | http://172.31.6.5:7689/posts/bulk (크롤러 호출 경로) |

---

작성일: 2026-05-12
변경 사항이 있을 때마다 갱신해주세요.
